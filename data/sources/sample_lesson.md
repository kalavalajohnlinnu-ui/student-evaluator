# Lesson 1: Introduction to Web Basics (Placeholder)

Welcome to the learning platform! This is a placeholder source file. 
When you provide your web details or URLs, they will populate or replace this file.

## Topics Covered
1. **HTML Elements**:
   - `<h1>` to `<h6>` heading tags.
   - `<p>` paragraph tags.
   - `<a>` link tags with `href` attribute.
   - `<button>` clickable buttons.
   - `<input type="text">` text input fields.

2. **Basic JavaScript Interactions**:
   - Declaring variables with `let` and `const`.
   - Selecting an element using `document.getElementById()`.
   - Adding a click event listener: `button.addEventListener('click', ...)`.
   - Changing text content: `element.innerText = "New Text"`.

## What is NOT Taught Yet (Intentional Gaps for Testing):
- No database or backend connections.
- No `fetch()` or external APIs.
- No `localStorage` data persistence.
- No styling/CSS layout systems (like Flexbox or Grid).
